import { createTemplate } from "../_shared/createTemplate";

/** Template case-study — caso de éxito: contexto → números grandes → resultado → CTA. */
export const CaseStudy = createTemplate("case-study");
