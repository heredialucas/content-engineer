import { createTemplate } from "../_shared/createTemplate";

/** Template website-demo — demos de sitios/apps: showcase con barra de URL + features + CTA. */
export const WebsiteDemo = createTemplate("website-demo");
