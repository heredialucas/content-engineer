import { createTemplate } from "../_shared/createTemplate";

/** Template portfolio-showcase — landing/portfolio: hook + screenshots + stack + métricas + CTA. */
export const PortfolioShowcase = createTemplate("portfolio-showcase");
