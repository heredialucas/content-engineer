import { createTemplate } from "../_shared/createTemplate";

/** Template product-promo — lanzamiento de producto: hook + demo + beneficios + CTA. */
export const ProductPromo = createTemplate("product-promo");
